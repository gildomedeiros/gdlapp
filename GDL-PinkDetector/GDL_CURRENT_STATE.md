# GDL current state

Updated 2026-09-14 (Australia/Brisbane). Current source version: **0.15.12.11**, versionCode **50**. User-supplied results reviewed below; installed APK provenance is unverified. No commit, push, tag or GitHub Release created.

## v0.15.12.11 local preset and Spotlight loss confirmation

Implemented against HEAD 7a05b916e752c8a65efaacf650d8717ee38fdbec plus documentation changes. PRODUCTION is hidden in UI; new installations default to COMBINED_REAL. Saved PRODUCTION selection migrates to Combined while preserving saved settings (including cooldown and recovery enablement); other saved presets remain. Spinner uses filtered list indices, not enum ordinals.

Configurable pin persistence defaults to 800 ms, no-pink-in-box timeout to 3000 ms (both 100..30000 ms). Fresh Combined captures evaluate controls/pin, so timing is bounded by actual processed-frame cadence, not guaranteed exact dispatch at the configured millisecond. Pin/control loss breaks pin persistence. Native solid green rail/corner detector rejects ambiguous/missing boxes and dotted pin boxes; pairing candidates capped at 256. Pink uses existing association HSV thresholds and 1280-reference pixel-count normalization inside box. No-pink timer arms only after pink is present in that box, resets on pink return, and disarms on unknown/changed box (adjacent-frame IoU below .3). Does not require a person icon or person model. Partial occlusion, differing layouts, background pink and false solid-box matches remain limitations; no accuracy percentage or device performance guarantee established.

Pending loss confirmation blocks acquisition/gimbal recovery. Confirmed pin or no-pink absence reuses guarded red-X exit and fresh two-frame exit verification; pink return cancels no-pink exit. Log includes control/pin/box/pink count, arming/pending flags, cancellations, confirmed reason and exit requests, plus settings snapshot. Rectangle acquisition, cooldown defaults, native CV and nonblocking saving retained. Logs remain Documents/GDL/<run>/diagnostic.log.

Validation: offline assembleDebug passed, APK metadata .11/50; PersistenceGate host test passed exact 800/3000 boundaries, interruption, restart and clock rollback. Host compiled-detector replay on 5ecdc022 and 0463d71e detected solid boxes with normalized pink counts 110/334; c0c47011 dotted pin frame returned no solid box. Uses desktop Bitmap/Color shims, not Android runtime. No real failure sequence or on-device run supplied for new no-pink behavior. Diff whitespace checked. Only local project files/build output changed; no install, commit, sync or publication.

## v0.15.12.10 repackaged from restored .9

Diagnostic log review (2026-09-14): supplied C:/Users/gildo/temp/diagnostic.log.txt contains one RUN_START reporting .10, preset=PRODUCTION, activeTrackAction=OFF, gimbalRecoveryEnabled=false and retryCooldownMs=3000. All 113 captures have PROCESSING_END; 12 TAP_REQUEST and 12 TAP_ANDROID_COMPLETED entries, no Spotlight exit requests, control_ms/pin_ms evaluations, ERROR/TIMEOUT/PROCESSING_SLOW/EVIDENCE_SKIPPED matches. Log ends after frame 113 evidence writes; does not cover historical F170–171 or establish why recording ended. Checked current HEAD 7a05b916e752c8a65efaacf650d8717ee38fdbec and processScreenshot routing: pin/control exit logic is reached only for COMBINED_REAL; this PRODUCTION configuration bypasses it. Thus this run does not exercise the detector replayed above, explaining absent automatic pin exit under its recorded configuration. Android completion is not proof of DJI response; screenshot-to-log frame correspondence and installed APK provenance remain unverified. Read-only source/log review; documentation only, no app/build change.

Clipboard detector replay: current compiled ActiveTrackPanelDetector.spotlightControlRect and Java SpotlightPinMatcher run against previous b6fcf845/9e6b0efd pair and latest eight 696274c0,c0c47011,c53852b1,0463d71e,0ce435a5,ceb44944,a9e6b883,23e2a070 screenshots at native 2340x1080. All ten returned red-X/chevron control. Previous pair pin=false/true; latest eight pin=false,true,true,false,false,false,false,false. Thus fresh evaluations supply exit condition for latest images 2/3, healthy-control condition for remaining six. Host replay uses production compiled detector with minimal Bitmap/Rect shims and Java RGBtoHSB in place of Android HSV; not an Android runtime/device-dispatch replay. Java pin matcher runs unchanged. Harness in build/desktop-replay. No app behaviour/version changes. These results do not establish installed APK, timing/cached state or actual exit dispatch; matching log is needed for those.

User requests .10 with logging, local project only, following rollback. This .10 retains restored .9 rectangle acquisition, 1000 ms Combined default, nonblocking evidence, capture timeout protection and persistent diagnostic logging. It does NOT restore the earlier rejected .10 centre-tap change. Only version fields and documentation changed from the restored .9. Historical .10 tap notes below are superseded. Offline assembleDebug passed; APK metadata confirms .10/49 and diff whitespace checked. No installation or synchronization.

## Requested rollback to v0.15.12.9

User reports .10 is worse and requests .9. Reversed .10 centre-tap changes locally: restored centred 3x3 marker rectangle/300 ms drag, 1000 ms Combined default, rectangle evidence/settings labels and .9/48 version fields. Existing saved cooldowns remain unchanged. Nonblocking saving, diagnostic logs and capture protection retained. Offline assembleDebug passed; metadata checked and diff whitespace checked. No device install, worktree synchronization or publication. Historical .10 notes below describe the reverted change.

## v0.15.12.10 restore centre-tap acquisition

User reports rectangle acquisition locks DJI onto a fixed position; no new device replay establishes causation. Requested revert implemented locally against HEAD 0068bd2 plus existing changes: Combined now dispatches a stationary tap at the current validated plus centre, using configured tap duration (default 120 ms), instead of the 300 ms diagonal rectangle. Default Combined retry cooldown 500 ms; saved settings preserved. Acquisition-in-flight exclusion, pink validation, fresh-frame coordinates, Spotlight veto/exit and recovery arming remain. Evidence/settings/log messages now describe taps. .9 nonblocking saving, capture tickets and persistent diagnostics retained; no CV changes. Legacy rectangle helper/test remains unused by service.

Validation: offline assembleDebug passed, .10/49 metadata checked; source inspected for removal of rectangle construction/dispatch in Combined, mapped centre coordinates and retained guards. Diff whitespace check passed. No device test, install, worktree sync or publication. A 500 ms minimum cooldown does not guarantee 2 Hz processing; moving-subject misses remain possible.

## v0.15.12.9 nonblocking evidence and diagnostic log

Local-only authorized fix against HEAD `0068bd2a6112ddb8c4bce50333a6a5547500186a` plus prior changes. EvidenceAdmission.tryAcquire replaces indefinite semaphore acquire; saturated image queue skips immediately and logs/counts skipped images in subsequent statuses. Image memory remains bounded to three retained copies. The writer may remain blocked but cannot block admission/detection. CaptureTicket IDs expire waiting callbacks after 10 seconds and reject late/duplicate arrivals; processing state is released in finally and overlay restored after processing/errors. Slow processing is reported at 10 seconds without launching overlapping processing/gestures. This does not forcibly recover a hung detector or Android main thread. No native-resolution, rectangle or gimbal-rule change.

DiagnosticLog has a separate bounded 256-entry queue with immediate rejection/drop counting. Records timestamps, frame context, capture start/callback/timeout/late discard, processing stages/errors, saving/skips/timing, status and tap/drag request/Android callbacks. Public output Documents/GDL/<run>/diagnostic.log and app-external-files diagnostics/<run>/diagnostic.log backup; public log may be unsupported, and storage/process termination may lose queued final events. Logs are separate from Pictures ZIP and should be supplied with evidence. Stage/gesture outcomes remain software observations, not proof of DJI response. Existing F170–171 causation remains unconfirmed.

Validation: blocked-writer host test exercised 10000 immediate failed admissions while writer stalled, then successful admission after drain. CaptureTicket tests passed for timeout, late callback rejection, duplicate callback and processing isolation. Final offline assembleDebug passed; metadata confirms 0.15.12.9 / 48. Diff whitespace check passed. No device test/install/worktree sync/publication.

## v0.15.12.8 rectangle acquisition and performance work

Reported F170→F171 silence (2026-09-14): user supplies two RAW/annotated clipboard pairs, airborne with expanded Spotlight/person box and later grounded with Android bars; reports no messages/files in between. Clipboard filenames do not establish frame numbering, duration, installed version or crash cause; run ZIP/logcat not supplied. Source review confirms .8 evidenceSlots.acquire() has no timeout: a blocked MediaStore writer can fill three slots and stall detection while screenshotInFlight remains true and overlay is hidden. Screenshot callback wait also has no watchdog, and flag release is not in finally. These are plausible stall paths, not established device causation. Expanded Spotlight remains outside collapsed-control detection (explains ABSENT label separately). Review/documentation only; no new fix/build. Need this run's archive/settings/timestamps and GDL_PERF/crash logs to distinguish storage stall, callback/worker failure and service restart.

User-authorized local-only implementation against HEAD `0068bd2a6112ddb8c4bce50333a6a5547500186a` plus existing changes. Combined acquisition uses a fresh validated marker's centred 3×width/3×height rectangle, 300 ms diagonal drag, symmetric edge reduction within the established preview. Single acquisition in flight; default Combined cooldown 1000 ms, saved settings preserved. Completed gesture enters existing awaiting-Spotlight monitoring and arms recovery as before, not proof of DJI selection. Pin exit/verification, native-resolution requirement and gimbal timer/single-attempt rules retained. Other standalone/diagnostic gestures retained.

Performance implementation: thread-local weak-identity/generation native pixel cache reuses one array across detectors; pink HSV computed only in the existing association windows, with unchanged count/weight normalization; necessary RGB prefilter before original plus HSV; pin sample offsets precomputed without changing samples/thresholds/search positions; removed red component per-pixel neighbour arrays (existing fixed ROI retained). Evidence bitmap copies enter a serial background writer, max three retained copies; saturation backpressures producers without deliberately dropping evidence. Queue acknowledgement is not save completion; failures reported asynchronously. Original-resolution annotation/copy work remains and device latency is not yet measured. Logcat tag GDL_PERF records stage/capture/dispatch/write timings; these logs must be collected separately from JPEG results.

Validation: rectangle geometry/edge tests, existing S11 countdown and single-attempt tests passed. Optimized pin matcher exactly matched previous native matcher on all 85 RAW frames in supplied .7 ZIP (three positive classifications); this is equivalence, not proof of marker accuracy. Final offline assembleDebug passed; APK metadata confirms .8 / 47. Source diff whitespace checked. No install or worktree synchronization. Next: device rectangle acceptance and measured throughput/latency comparison; expanded Spotlight layout, occluded pin and telemetry limitations remain.

## v0.15.12.7 native-resolution CV and Spotlight exit

F39 follow-up: inspected F35, F36, F37, F38 and F39 RAW, plus F38 annotation. F35 exit request still has red X/chevron and pin; F36–39 no longer show those controls. F38 reports no validated plus/control absent, despite a person with pink being visible without an acquisition plus. F39 S12 is consistent with the agreed recovery conditions after exit verification; pink/person visibility alone is not a reset signal. ZIP writes F35 12:28:26, F39 12:28:42 (~16 s), consistent with countdown running during exit, not proof of exact timer start/expiry. Request correctness is separate from actual DJI gimbal response. Documentation only.

Device-results performance review: `C:/Temp/te1/20260914_122605_068_COMBINED_REAL.zip` has 137 JPEGs (settings plus evidence), 85 RAW captures, settings version 0.15.12.7, search interval 400 ms, every-capture saving enabled, retry 800 ms, gimbal hold 2000 ms + drag 30000 ms. Across 45 consecutive normal-search RAW pairs (S01/S02/S11), ZIP modification-time differences average 4.133 s, median 4 s: 36 at 4 s, six at 6 s, three at 2 s. Approximate effective saved-search rate 0.24 Hz, about 90% below configured 2.5 Hz. ZIP timestamps have two-second granularity and describe file writes, not exact capture/CPU timings. S13 F40–68 spans 28 seconds for 28 intervals (~1 Hz), consistent with much less CV work during gesture. One S12 request F39 and one S17 F69; three exit requests and two verification-stage frames. Earlier 0.15.12.2 archive's comparable 167 pairs average 1.114 s (~0.90 Hz): latest is ~3.7 times the interval (~73% lower rate), but runs differ in scene/version/workload and are not a controlled resize-only benchmark. Current worker serializes screenshot processing and image saving; expensive native CV and allocations are plausible contributors, not individually profiled. No CPU, thermal, memory, capture timestamps or per-detector latency logs in archive. Review-only; app unchanged. Exact timing instrumentation is the next step before choosing optimization; original-pixel CV remains the user's requirement.

Applied to local project only against revision `0068bd2a6112ddb8c4bce50333a6a5547500186a` plus existing changes. Removed all app-controlled createScaledBitmap paths in plus, pink association/blob, FocusTrack, legacy knob and telemetry OCR (including OCR enlargement). OCR library internal processing is outside this guarantee. Pink association counts/scores are normalized to the previous 1280-width reference area so the existing minimum does not become easier solely from pixel count; colour tests remain unchanged. Other structural ratios use native geometry; pixel minimums remain floors. Full-resolution processing costs more and has not been benchmarked on device.

Added SpotlightPinMatcher: native-pixel green composite template derived from supplied e2377c2b screenshot, checking positive and negative samples spanning circular X, dotted box and pin. Template coordinates follow screenshot dimensions; screenshot pixels are not resized. This is reference-layout dependent, not a universal DJI semantic classifier; Fn overlays/occlusion and other UI scales need further testing. Existing collapsed control now returns its rectangle for exit targeting. Chevron arm extent changed from 12% to 7.5% of button width, bright-neutral minimum from 150 to 120, and vertical tolerance from one to two pixels for native grey strokes.

Combined flow checks marker/control at the existing 1-second interval or before candidate taps. Healthy control means red X/chevron present and pin absent. Both present latches exit verification, requests the detected red X under current DJI/fullscreen/action guards, and blocks acquisition/recovery until two fresh checks find both control and pin absent. Exit taps may retry at max(1000 ms, configured acquisition cooldown); gimbal retries remain disabled. Callback is not exit proof. If healthy Spotlight returns, exit pending clears without a cancellation tap. Absence countdown runs during exit (unless validated plus or healthy control resets it); original recovery arming remains required. New run resets exit state. Pending verification stages are saved. Expanded mode recognition remains outside the collapsed control detector; no claim this solves that separate layout case.

Validation: final offline assembleDebug passed, version 0.15.12.7/46. Native Java matcher replay: e2377c2b and bdb88271 positive, 1620da39 and 39b91cf7 normal selected-person negatives; desktop 15–68 ms (not device FPS). Pillow native-pixel chevron predicate replay on 0e4b35dc finds matching arms; not Android end-to-end replay. Existing countdown and single-attempt regressions rerun. Source resize scan and diff whitespace checked. No install, worktree sync, commit or release. Device validation remains required for exit acceptance, full CV latency, expanded menus, occlusion and false positives.

## v0.15.12.6 approved acquisition retry correction

Additional 29-image clipboard sequence reviewed 2026-09-14: expanded Spotlight menu frames report no validated plus / Spotlight control ABSENT; image 8 still visibly encloses the person in a green selection box. Collapsed-control images 12, 13, 15, 17, 20, 22 and 23 also report ABSENT despite visible red X/chevron. Image 16 records S12 recovery requested, hold 2000 ms + drag 1000 ms, while red control and pin marker remain visible. Current code's false control result permits the armed absence countdown to expire; expanded layout is not recognized by this specific detector. Repeated collapsed ABSENT evidence supports a control-detection failure (individual results may be cached), not an intentional pin-triggered recovery: pin detection/exit is not implemented. Exact failed predicate, run timeout/timestamps and installed build remain unverified without original run evidence/replay. S12 is request evidence, not DJI movement proof. Annotation retains legacy RED LIMIT wording unlike intended timed evidence, so build provenance warrants checking. Reviewed current detector and S11 timer; documentation only, no app changes.

Follow-up observation (2026-09-14): user reports Spotlight remains stuck after losing the subject, drag-down has no effect in that state, and another person may be selected. Supplied clipboard e2377c2b shows the large red X/chevron plus a small green X attached to a dotted box containing a map-pin icon over the roof. This single frame does not establish the icon's universal semantics or input-response causation. Current service resets absence timing on Spotlight-control visibility without distinguishing this marker. Proposed only: detect the combined green-X/dotted-box/pin marker, exclude that state from the Spotlight timer reset, exit via detected large red X, verify exit in fresh frames before recovery, and preserve pink-validated acquisition. No implementation authorized by the request for thoughts; no CV/code change. Reviewed current S11/control-veto source and supplied image; device validation of marker discrimination remains needed.

Applied locally only, against checked revision `0068bd2a6112ddb8c4bce50333a6a5547500186a` plus existing working changes. Removed the combined COMPLETE existing-plus-must-clear gate. After the configured cooldown, a persistent validated candidate can be tapped using coordinates from the current screenshot, never saved coordinates from the previous attempt. The existing red-X/chevron detector now also evaluates candidate frames before each eligible tap; a visible control suppresses tapping and resets countdown. Missing-plus S11 checks retain the 1000 ms minimum interval. No detector algorithm/threshold changes. Screenshot-to-dispatch movement can still cause misses.

Android completion now reports awaiting Spotlight control instead of claiming selection. Completion continues to arm recovery and enter monitoring; neither signal starts the configured absence countdown, and either signal clears it. Single timed gimbal attempt, no automatic gimbal retry, duration settings and arming rule remain unchanged. Acquisition retries are distinct from gimbal retries.

Validation: offline assembleDebug passed, output metadata 0.15.12.6 / 45; standalone S11RecoveryCountdownTest and SingleGimbalAttemptTest passed. Source reviewed for fresh decision coordinates, cooldown before retry, control veto before dispatch, and removal of stale plus-clear gate. No device test/install; chevron classification accuracy and actual DJI tap acceptance remain unverified. Documentation/source whitespace checked. Worktree unchanged.

## Supplied clipboard results reviewed 2026-09-14

Reviewed the 15 supplied images (clipboard filenames beginning 07925244, a8e2cc9a, 6f76bb12, 105853c8, b0a96e7d, 6b4193f3, 0fd50c93, c31a965b, 25bfeecd, 4aee71c4, 288c8615, 0238b8c5, 8027542a, ab2ebd9a, c0e2c3b8). Annotations report selected plus candidates with 451–539 pink pixels. RAW pairs retain the acquisition green-plus icon; no selected-subject box or Spotlight red-X/chevron control is visible. Later images show a yellow focus/exposure box below the plus. These show detection without visible DJI selection, not proof of dispatched coordinates or gesture outcome. The top remaining-flight estimate is not a reliable elapsed-time clock.

Source checked at revision `0068bd2a6112ddb8c4bce50333a6a5547500186a` plus current 0.15.12.5 working changes: `processCombinedReacquire` sets COMPLETE and `combinedSawNoPlusSinceComplete=false` after Android tap completion, without verifying DJI selection. The existing-plus-must-clear gate then suppresses another plus tap while that plus remains, although evidence still labels its detected candidate SELECTED. This is a confirmed logic gap consistent with the supplied sequence; exact runtime stages require original run evidence. The callback message claiming Spotlight selected is unsupported by that callback alone. A missed or stale-coordinate tap is a hypothesis suggested by the displaced focus box, not established causation. Validated-plus presence also resets recovery timing, so these frames do not exercise the chevron detector or missing-target recovery.

Validation: visual review of supplied images and current tap/COMPLETE gate source. Documentation only; no app, CV, version or APK changes. Next step: inspect original run stages and tap mapping/coordinates, then propose a bounded selection-confirmation or retap change for approval.

## v0.15.12.5 approved Spotlight-only flow

Applied to `C:/Users/gildo/gdlapp/GDL-PinkDetector` only, against revision `0068bd2a6112ddb8c4bce50333a6a5547500186a` plus the existing 0.15.12.4 working changes. Worktree not synchronized. Approved change supersedes the earlier proposal to relax the ActiveTrack darkness threshold: the existing ActiveTrack classifier is unchanged, and its separate diagnostic presets remain, but combined production now bypasses that coordinator entirely. Successful validated-plus tap goes directly to COMPLETE/S11 and leaves DJI in default Spotlight. Combined settings load ActiveTrack action Off, defaults/UI agree, and the service no longer requires or dispatches an ActiveTrack action in this flow.

The approved Spotlight-control detector reuses existing red-control extraction and X classification, with a new bright-neutral upward-chevron arm check above that control. No dark-upper-panel prerequisite or OCR is used for this gate. S11 caches this visibility at a 1000 ms minimum check interval; the default plus search remains 400 ms. Either validated plus/pink or control visibility resets the absence countdown. Arming still requires a successful validated tap, and the configured absence timeout remains 15 seconds by default. S11 evidence now includes cached Spotlight-control visibility. This new chevron check is not yet device-validated; false negatives/positives remain possible.

New persisted `gimbalDragDurationMs` setting: default 6000 ms, bounded to 1000–30000 ms in UI/load/save, used in isolated and production gesture dispatch and evidence. Hold stays 2000 ms, coordinates unchanged. `SingleGimbalAttempt` consumes the attempt before dispatch guards, so completion, failure, rejection and guard/map failure cannot cause automatic retries. Production disarms on attempt and resumes plus searching after its outcome; only a new successful validated tap re-arms it. Isolated test requires a new run. No service calls to `GimbalKnobDetector.evaluate()` remain; red confirmation and angle claims were removed from timed evidence. Detector types/constants remain for geometry/legacy code, not live red detection. No RTH/landing lock added.

Validation: standalone `SingleGimbalAttemptTest` and existing `S11RecoveryCountdownTest` passed; reviewed combined bypass, successful-tap transition, attempt consumption/reset, settings persistence/defaults, duration propagation and absence of red-evaluation calls. Build encountered a settings encoding issue which was corrected; final offline `:app:assembleDebug` passed and APK metadata confirms 0.15.12.5 / 44. `git diff --check` passed. No device testing or installation. Future test priorities: real collapsed red-X/chevron visibility, missing/Stop/expanded controls, duration calibration, no retry after all outcomes, and rearming after fresh validated taps.

## v0.15.12.4 approved S11 monitoring

Supplied results reviewed for run `20260913_133438_072_COMBINED_REAL` (F31–35 RAW plus F31/F32 annotations, and F0 settings). Settings report 0.15.12.4 with 15-second timeout. F31 visibly retains green selected-subject box and ActiveTrack/Stop; F32 already shows “Auto returning to home” in the RAW request frame, with the box gone; F33–35 show RTH while recovery is in progress, and F34/F35 display gimbal angles −55°/−56°. Thus RTH is visible before this requested gesture is dispatched, not established as caused by it. Battery display is 17%; RTH trigger remains unknown. Filename telemetry is UNKNOWN despite visible 8.8 m height and 27→24 m distance.

Diagnostic finding against checked revision `0068bd2a6112ddb8c4bce50333a6a5547500186a` plus current 0.15.12.4 working changes: existing running detection requires the upper panel dark-pixel ratio >=0.30 (pixel HSV value <=0.32). An approximate Pillow bilinear 640×295 replay of that exact F31 region gives 0.0043, far below threshold. This supports a brightness-related running false negative even with Stop visible; it is not an Android detector replay or a logged cached decision. S11 annotations omit the running result, metrics and countdown age, so F31's “no validated green +” alone cannot prove a false result. Earlier countdown history and actual Android replay remain to be checked. This supplied evidence supersedes the earlier lack of 0.15.12.4 device results, without establishing APK hash provenance. Review only, no CV or app changes; documentation diff checked.

Future enhancement added 2026-09-13: keep Spotlight mode when ActiveTrack is unavailable because altitude or distance is insufficient; tracked in [ENHANCEMENTS.md](ENHANCEMENTS.md). User-requested backlog item, not verified DJI thresholds or implemented behaviour. Documentation-only update at checked revision `0068bd2a6112ddb8c4bce50333a6a5547500186a` with the existing 0.15.12.4 changes present; reviewed wording and ran `git diff --check`. No app/version change.

Applied directly to the original local project, based on clean revision `0068bd2a6112ddb8c4bce50333a6a5547500186a`. In combined COMPLETE/S11, absent-plus frames now reuse `ActiveTrackPanelDetector.evaluate()` at most once per 1000 ms (next available screenshot, not guaranteed exact FPS). The running result is cached between checks. Default 400 ms plus capture/search scheduling stays unchanged. No new CV/OCR or detector changes; this use does not dispatch any proposed ActiveTrack tap.

`S11RecoveryCountdown` is timing-only. It cannot start before successful validated-plus-tap arming; it starts on the first S11 observation with neither validated plus/pink nor a running classification. Either detection clears the countdown, and the next absence starts a full configured timeout, default 15 seconds. The existing last-plus and guard-delay gates remain additional conditions. Run, new ActiveTrack cycle and guard resets clear S11 timing/cache. Active recovery bypasses this check as before. Standalone production and gestures remain unchanged. The earlier 0.15.12.3 notes below describe the prior version; S11 now additionally requires this absence countdown.

Known limitation retained: F420 shows the existing detector can classify running despite a subject-lost message; recovery will stay inhibited until the running classification clears. Device validation remains required for F121/F122 and later loss transitions. The green tracking box itself is not newly detected.

Validation: standalone Java regression test `tests/S11RecoveryCountdownTest.java` passed for never-armed startup, prolonged running, initial loss, exact 15-second boundary, plus refresh, running refresh and reset. These tests validate timing, not image classification. Source diff reviewed for S11-only gating and unchanged detector sources. Offline `:app:assembleDebug` passed using the existing JBR/Gradle cache; APK metadata confirms 0.15.12.4 / 43. `git diff --check` passed. No device test, install, commit or publication performed.

## v0.15.12.3 approved timer-only change

Based on checked revision `180a9a4e19aa82dafc77930464e074bfa084afe4`, preserving the prior result-review documents. The earlier unapproved selected-box/OCR implementation was reverted. User explicitly requires approval before any new CV logic; this version adds none. The approved scope is elapsed time since the latest existing green-plus/pink validation, with already-active recovery unchanged. It does not implement a selected-subject/Stop veto and therefore does not fix the F121/F122 tracking-box case.

`evaluatePlusAndRecordDetection()` wraps the unchanged pipeline and records the current capture-processing elapsed timestamp only when the existing `isValidatedGreenPlusWithPink()` predicate passes. It also records detections during tap cooldown. All four service pipeline-evaluation paths use that wrapper. The recovery timeout now compares against that timestamp rather than creating a timer on the first absent-plus frame. The successful validated-tap arming requirement, preset/phase guards, bottom latch and active-recovery priority remain. Run reset clears the timestamp; ordinary selection reset preserves it. A separate `recoveryNotBeforeMs` retains a timeout-length retry delay after guard failure, coordinate-map failure or dispatch rejection without inventing a detection. A later valid detection replaces that delay with its own full timeout.

Validation: `:app:assembleDebug --offline` succeeded using the existing Android Studio JBR and Gradle cache. APK output metadata confirms 0.15.12.3 / 42. Reviewed all detection call sites, timestamp writes, cooldown ordering, arming, run reset and active-recovery bypass; verified no detector/OCR source changes and no `DjiTrackingSelectionDetector.kt`. `git diff --check` passed. No device replay or automated behavioural test performed. Next step is device validation of the last-detection timeout and refresh during cooldown. Original project is the edited source; the previously mirrored worktree remains at its reverted snapshot unless separately synchronized.

## v0.15.12.2 full-screen requirement

Worktree change based on `4025216b39daeb5535421a68a8582a2345409a92`: all preset defaults and loaded/saved settings now require DJI full-screen; the UI switch is checked and disabled. The service guard no longer honors a false flag for DJI-targeted runs, including Raw DJI diagnostics. Gallery/other test foreground targets remain exempt from this DJI-specific guard. No change to the existing navigation-bar detection algorithm, gesture coordinates, or timings. Version fields were advanced together to 0.15.12.2 / 41 for this requested source release. Applied to the original folder on 2026-09-13, preserving its existing changes. Original checkout was clean on main at 31ce12019c0a407de1886dc39b8872539e07eb48 before transfer. Copied files verified by SHA-256; no release commit or device test performed.

Validation: inspected all preset defaults, persistence, UI construction and the central guard; `git diff --check` passed. Offline `:app:assembleDebug` could not resolve Android Gradle plugin 8.7.3 in the isolated cache, so compilation remains unverified.

## 0.15.12.2 supplied device results (2026-09-13)

Reviewed archive `20260913_105318_241_COMBINED_REAL.zip` against clean source revision `180a9a4e19aa82dafc77930464e074bfa084afe4`; detailed findings and provenance are in [RESULTS_0.15.12.2_20260913.md](RESULTS_0.15.12.2_20260913.md). This supersedes the earlier absence of supplied 0.15.12.2 device evidence, but does not establish APK hash/build provenance or a locally reproduced device test.

Verified archive inventory: 538 logical captures, 821 JPEGs including settings. Settings report 0.15.12.2 and mandatory full-screen=true. Recorded 27 recovery requests, 26 Android completion records, four distinct bottom confirmations (F299/F384/F408/F438), six S08 running classifications and two ActiveTrack timeouts. Selected raw images support the four DJI bottom-limit episodes. F420 nevertheless says “Subject lost. Reselect subject” while GDL records running/success. F126 shows Land UI during the first recovery; F495–508 show Land UI again; F509/F522 request recovery while DJI says “Tap to cancel landing”; F535 requests recovery while DJI says “Takeoff permitted” and displays 0.0 m. Landing/input causation remains unresolved. F537–538 show Android bars during an existing gesture; no subsequent request tests guard rejection. OCR mismatch remains confirmed (e.g. F509 raw height 3.4 m vs filename 0.6 m).

Validation: all filenames/dimensions counted, selected raw/annotated frames and complete specified transition sequences visually inspected, relevant recovery/gesture/full-screen/ActiveTrack code read, documentation diff checked. No app/version change or build. Next investigations: dispatched coordinate mapping/input provenance, recovery despite DJI target/Stop UI, and running classification despite subject loss. No landing lock or other behaviour change authorized by this result review.

F121→F122 follow-up at the same checked revision: both RAW frames retain a green selected-target box/X and ActiveTrack/Stop controls, with no visible subject-lost warning or Land panel. F121 annotation says “no validated green +”; F122 requests recovery. Direct image comparison and rereading the no-plus timer confirm the trigger mismatch: lack of a fresh acquisition plus starts recovery despite DJI's selected-target/running UI. This is separate from later landing causation. Documentation only; diff check passed.

## Original checkout and sources

- Transfer note (2026-09-13): these notes were prepared in the Codex worktree described below and copied to `C:\Users\gildo\gdlapp\GDL-PinkDetector`. The original project is on `main` at the same revision, with no tracked changes before transfer. It contains untracked Gradle wrapper/build and IDE files; these were preserved. Statements below about a clean detached checkout and missing tracked wrapper refer to the inspected worktree, not an absence of local build tools in the original folder.
- Checkout: **detached HEAD**, revision `4025216b39daeb5535421a68a8582a2345409a92`. Local `main` and `origin/main` contain this revision; neither was checked out or refreshed from the network. Working tree was clean before these documents.
- App: **0.15.12.1**, versionCode **40**, verified in `app/build.gradle.kts`; `GdlTestSettings.APP_VERSION` and `activity_main.xml` agree. Installed APK identity is unknown.
- Historical source: ChatGPT **GDL-PinkDetector-v0.15.12.1**, conversation `6aa4fbd6-f01c-83ec-9c2c-aa47ea5301b1`. Retrieved with `read_thread`, ten turns per page, following older cursors through the v0.15.7 design discussion (turn `cbe9913c-fd23-4e94-ab10-186283b98764`). Older history remains available; this is a focused recovery, not a complete transcript. Some long historical responses were truncated. Attachments were listed but their images/archives were not independently inspected here.
- Code paths below are relative to `app/src/main/java/com/gdl/pinkdetector/`. `README.md` and `VALIDATED_LOGIC.md` provide additional project history, not independent device validation. No pre-existing applicable ancestor or project `AGENTS.md` was found.

## Agreed behaviour and code verification

The final agreed v0.15.12.1 scope superseded the proposed v0.15.13 name, X=24% suggestions, and landing-detector/landing-lock proposals. It retained recovery and moved both gestures to X=79.1%, simplified gimbal detection to the red limit, and corrected evidence. Source: user-pasted scope in turn `7e7892f8-f627-4a4d-ace1-2cb5685f509a`, version correction `9254f14c-ff95-40a6-ba0d-419aacdb33ec`, and clarified scope `6a0ac405-d7fa-404c-b4b1-bd9d15c1a4b8`.

| Area | Verified in current code |
| --- | --- |
| Main loop | `GdlAccessibilityService.captureLoop` calls `captureIfReady()`, then reschedules using `currentCaptureIntervalMs()`. Screenshot processing routes through `processScreenshot()` to the relevant detector/coordinator. |
| Gesture coordinates | `GimbalKnobDetector`: `FIXED_X=0.791f`, `GESTURE_START_X=FIXED_X`, `GESTURE_START_Y=0.45f`, `DRAG_BOTTOM_Y=0.79f`. Both `processGimbalTest()` and `processProductionGimbalRecovery()` multiply these by screenshot dimensions and call `dispatchGimbalDrag()` with equal start/end X. |
| Touch implementation | `dispatchHoldThenDrag()` dispatches a stationary 2-second stroke with continuation enabled, then `continueStroke()` for a 6-second downward path. This represents one continued pointer across **two dispatch calls**, not a second pointer at a white-ring location. `mapSourcePointToView()` scales screenshot coordinates to overlay dimensions. Actual device mapping remains unmeasured here. |
| Test start/stop | Normal readiness delay is 3 seconds; gimbal test then has a guard-dependent 10-second countdown. Red is evaluated before countdown/in-flight/rest branches. Two positive red evaluations latch completion; a negative evaluation resets the count. About 13 seconds to first hold is conditional on guards/capture scheduling, not a guaranteed start time. |
| Retry | 6-second rest after the gesture callback; repeated down-only gestures until red confirmation. No white/green knob or dashed-line visibility prerequisite. No attempt ceiling in these recovery loops. Completion prevents further gestures; already dispatched movement is not explicitly cancelled by red detection. |
| Production arming | Starts disarmed on a new run. Successful real-tap completion with `isValidatedGreenPlusWithPink()` arms recovery; ActiveTrack success is not the arming event. Default recovery is On for `COMBINED_REAL`, Off for standalone `PRODUCTION`, with a configurable 15-second no-selected-target timeout. A no-pink selection does not meet the arming predicate. |
| Production coordination | `processCombinedReacquire()` evaluates no-target timeout in post-cycle waiting too. `gimbalGestureAllowed()` permits combined recovery outside `ACTIVE_TRACK`, including `COMPLETE`/S11. Active recovery takes priority over green-plus evaluation; after red confirmation it resumes searching once an in-flight gesture finishes. |
| Red detector | `GimbalKnobDetector.evaluate()` downsizes to at most 720px width, searches near X=79.1%, accepts compact red components centred in Y=72–84%, with a 2% vertical search margin. It classifies pixels, not a numerical angle from DJI telemetry. |
| Evidence | `GimbalEvidenceAnnotator` draws on a copied bitmap: red limit region, detected red knob, and cyan **requested** path when supplied. No full-height blue search rectangle. These annotation pixels do not dispatch touches or feed detection. |
| Guard limitations | Gimbal dispatch checks readiness, allowed preset/state, DJI foreground, mandatory full-screen setting, Android 11+, and the full-screen guard. There is no landing/RTH recognition or landing lock. Guard checking before the hold does not establish safety throughout the continued drag. |
| Settings/evidence | Combined defaults include 400ms search, 800ms plus retry, 120ms tap, minimum 8 pink pixels. Each run gets a settings snapshot and timestamped evidence folder. Hard-save saves successful captures, not guaranteed frames at every scheduled tick. |
| Telemetry | `scheduleTelemetryRead()` runs asynchronously at roughly 1-second intervals; filename creation reads `latestTelemetry` separately. Altitude/distance suffixes can be stale or differ between RAW/ANNOTATED images for one frame. They are not latitude or flight-state input. |

Existing green-plus/pink and ActiveTrack behaviour was intended to remain unchanged by the lane correction; historical byte-for-byte equivalence to old ZIPs was not established in this review. See `ReacquirePipeline.kt`, the plus/pink detectors, `ActiveTrackPanelDetector.kt`, and the existing project documentation before changing those features.

## Device-test observations reported in the conversation

These are attributed reports, **not fresh device verification**. Frame numbers are run-local and must be paired with version/settings.

- **v0.15.12.1 isolated test:** turns `cd32399b-1344-4994-8a61-6816ea99c070` and `884a0a66-2805-4d34-ac0e-d3ad9eff955c`: assistant interpretation of supplied F00012/S22, F00013/S26 and F00020/S26 images reported a 79.1% requested path, gimbal UI appearing, and movement from about 0° to −50°. Later image interpretation reported about −60°. No final S23/S24 sequence was recovered for this new-lane test.
- **v0.15.12 production run:** results turn `bf9eefcd-e2af-4c7c-8197-400359ad3959`, detailed frame mapping `647826e5-5d2f-4f81-8f87-658e744754ef`: reported 459 logical frames/787 images, no recovery in F1–169 before F170 successful pink-plus tap, 14 completed gestures, no S18, and four bottom-confirmed episodes: F212→233, F259→304, F327→374, F407→454. First ActiveTrack cycle hit its two-tap limit at F177; running confirmations were reported at F196, F243, F311, F391. These counts were not recounted from an archive here.
- **Landing concern in that older run:** same source reported Land/Update Home Point UI by F407, “Tap to cancel landing” at F427, and further recovery requests at F433/F446. User turn `adb2309c-2b87-4c8f-b5eb-408ff8866413` specifically raised overlap of the initial long press with landing controls and requested moving it while retaining recovery.
- **Earlier regressions:** v0.15.10 partial archive F263–291 reportedly stayed in S11 after ActiveTrack timeout despite insufficient pink (`9af715a4-5a73-46de-9e4f-e3252ac771b5`). v0.15.11 later requests reportedly failed the phase guard while leaving a misleading wheel-rest timer (`2578540e-8656-4de0-aaef-bb3ecb76b762`). Current code contains the arming/phase/timer corrections; earlier S12/S16 filenames alone are not proof of movement.
- **Gesture/detection history:** early immediate drags produced subject-selection rectangles; continued hold/drag was adopted. v0.15.9 results reportedly reached red at F58–59 but rest-state ordering missed completion (`a125d59c-d11a-4566-844f-dba776e836d5`). A bright-scene F77 reportedly missed the normal white knob and telemetry (`c696f147-163e-424b-b30d-1aefce37d382`). These explain the red-first ordering and removal of normal-knob gating; they do not establish universal red-detector accuracy.
- **Telemetry remains unresolved:** old runs reported missing decimals, speed mistaken for distance, negative/extra-digit values, UNKNOWN, and RAW/ANNOTATED disagreement. Retain raw images as the reference; do not infer altitude or landing causality from suffixes alone.

## DJI Fly exit/freeze review (2026-09-13)

Follow-up tracked in [ENHANCEMENTS.md](ENHANCEMENTS.md), added 2026-09-13 at checked revision `67b5802da25e127da5fcc81d6c5c226c1cf516c5`. Documentation checked against the review below; no app/version changes or device testing.

Static inspection at revision `67b5802da25e127da5fcc81d6c5c226c1cf516c5`: `captureIfReady()` stops starting captures when the configured DJI foreground package is absent and displays “ARMED • waiting for DJI Fly”; the service loop remains enabled. Tap dispatch paths and `gimbalGestureAllowed()` recheck foreground before dispatch. Returning to DJI can resume processing without a new arm action; foreground loss does not perform the full disabled-state reset. There is no DJI restart/watchdog implementation in the inspected Kotlin sources. A frozen DJI window that still matches the package is not detected as a crash by this guard. `dispatchHoldThenDrag()` does not recheck foreground between the hold completion and continued drag, nor explicitly cancel an in-flight gesture on foreground loss. Device behaviour during a crash, and aircraft/ActiveTrack behaviour, remain unverified. Validation: read capture loop, foreground checks, tap dispatch guards, countdown reset and continued gesture callback; searched Kotlin sources for launch/watchdog logic. No build or device test, no app behaviour change.

## Unverified claims, limitations, and next steps

1. **White ring / Land:** turns `1fda67f1-947c-473a-be45-4623d04e46f0` and `81f963dc-cb64-48c1-9f0a-c1f935955a6e` asserted DJI intentionally offsets a visual ring about 55px left and that the new path cannot touch Land. Earlier explanations alternated between angle bubble and touch feedback. GDL code establishes no second injected ring pointer, but cannot establish DJI hitboxes, ring semantics, layout invariance, or absolute safety. Claimed panel/button edges and 117px/420px clearances were not measured here. The old frame was explicitly identified by the user as old.
2. **Causality:** prior claims that GDL did not initiate landing, or that low battery caused it, are not proven by this review. Nor is the earlier claim that Android cannot capture screenshots during continuous gestures established; later reported in-flight frames contradict that categorical explanation.
3. **Evidence semantics:** S12 is requested, S13 is in-flight state, S17 is Android completion/failure, S18 is pre-dispatch rejection/cancellation, S16 is rest. None independently proves physical gimbal movement. Production outcome handling can occur between red evaluations, so “two consecutive frames” is more precisely two positive detector evaluations without an intervening negative evaluation.
4. **Validation still needed:** identify the installed APK/build and DJI/phone/display configuration; review complete new-lane raw+annotated test evidence through S23/S24 and production S14/S15. Check actual mapped coordinates, red detection, repeated recovery, guard transitions, and behaviour with relevant DJI UI states. Do not declare the new lane fully validated from F12/F13 alone.
5. **Separate follow-up work:** investigate OCR accuracy and per-frame telemetry consistency; reconcile overly broad safety/timing claims in existing README/history if documentation is revised. Clarify desired failure handling if red never appears. No landing detector, timeout cap, or behaviour change is newly authorized by this historical recovery.
6. **Build provenance:** no Gradle wrapper is tracked here; previous chat reported ZIP/static checks but no APK compilation. This review did not reproduce those checks or build the app. A suitable Android build environment and device evidence remain necessary before claiming build/runtime validation.
